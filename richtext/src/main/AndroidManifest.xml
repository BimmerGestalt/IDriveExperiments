<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.bimmergestalt.idriveexperiments.richtext">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IDriveExperiments">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <service android:name=".CarAppService" android:exported="true">
            <intent-filter>
                <action android:name="io.bimmergestalt.carconnection.service" />
            </intent-filter>
            <intent-filter>
                <action android:name="io.bimmergestalt.idriveexperiments.richtext.uiConnected" />
            </intent-filter>
        </service>
    </application>

</manifest>